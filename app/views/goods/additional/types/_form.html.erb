<%= form_for(@goods_additional_type) do |f| %>
  <% if @goods_additional_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@goods_additional_type.errors.count, "error") %> prohibited this goods_additional_type from being saved:</h2>

      <ul>
      <% @goods_additional_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label I18n.t(:active) %><br />
    <%= f.check_box :active %>
  </div>
  <div class="field">
    <%= f.label I18n.t(:remark) %><br />
    <%= f.text_area :remark %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
