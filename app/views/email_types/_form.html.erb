<%= form_for(@email_type) do |f| %>
  <% if @email_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@email_type.errors.count, "error") %> prohibited this email_type from being saved:</h2>

      <ul>
      <% @email_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :active %><br />
    <%= f.check_box :active %>
  </div>
  <div class="field">
    <%= f.label :remark %><br />
    <%= f.text_area :remark %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
