<%= form_for(@participant_contact_email) do |f| %>
  <% if @participant_contact_email.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@participant_contact_email.errors.count, "error") %> prohibited this participant_contact_email from being saved:</h2>

      <ul>
      <% @participant_contact_email.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :person_id %><br />
    <%= f.text_field :person_id %>
  </div>
  <div class="field">
    <%= f.label :email_user_id %><br />
    <%= f.text_field :email_user_id %>
  </div>
  <div class="actions">
    <%= f.submit I18n.t(:save)%>
  </div>
<% end %>
