<h1>Listing transaction_record_taxes</h1>

<table>
  <tr>
    <th>Transaction record</th>
    <th>Taxe value</th>
    <th><%=I18n.t(:active)%></th>
    <th><%=I18n.t(:remark)%></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @transaction_record_taxes.each do |transaction_record_taxis| %>
  <tr>
    <td><%= transaction_record_taxis.transaction_record_id %></td>
    <td><%= transaction_record_taxis.taxe_value_id %></td>
    <td><%= transaction_record_taxis.active %></td>
    <td><%= transaction_record_taxis.remark %></td>
    <td><%= link_to I18n.t(:Show), transaction_record_taxis %></td>
    <td><%= link_to I18n.t(:Edit), edit_transaction_record_taxis_path(transaction_record_taxis) %></td>
    <td><%= link_to I18n.t(:Destroy), transaction_record_taxis, :confirm => I18n.t(:are_you_sure?), :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Record taxe', new_transaction_record_taxis_path %>
