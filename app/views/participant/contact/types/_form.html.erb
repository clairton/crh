<%= form_for(@participant_contact_type) do |f| %>
  <% if @participant_contact_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@participant_contact_type.errors.count, "error") %> prohibited this participant_contact_type from being saved:</h2>

      <ul>
      <% @participant_contact_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :active %><br />
    <%= f.check_box :active %>
  </div>
  <div class="field">
    <%= f.label :remark %><br />
    <%= f.text_area :remark %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
